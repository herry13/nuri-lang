/**
 * An enumerable of all possible states.
 *
 * Notes that some states are applicable to an object, while others are not. 
 * For example: the applicable states of a 'Package' object are 'uninstalled'
 * and 'installed'. Other states such as 'running' are not applicable to this
 * object.
 */
enum State {
    not_exist,
    uninstalled,
    installed,
    stopped,
    running
}
